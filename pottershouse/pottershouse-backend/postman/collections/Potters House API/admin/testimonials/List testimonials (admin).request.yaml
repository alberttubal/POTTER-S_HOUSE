$kind: http-request
url: http://{{baseUrl}}/admin/testimonials
method: GET
headers:
  Accept: application/json
body:
  type: text
  content: ""
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/List testimonials (admin).resources/examples
order: 1000
