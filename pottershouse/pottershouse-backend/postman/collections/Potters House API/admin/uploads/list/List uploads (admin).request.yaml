$kind: http-request
url: http://{{baseUrl}}/admin/uploads/list
method: GET
headers:
  Accept: application/json
body:
  type: text
  content: ""
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/List uploads (admin).resources/examples
order: 1000
